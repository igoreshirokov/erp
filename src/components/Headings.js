'use client'
import styled from "styled-components";
import { colors } from "@/constants";

const H1 = styled.h1`
    color: ${colors.headingText};
    font-weight: 400;
`;
const H2 = styled.h2`
    color: ${colors.headingText};
    font-weight: 400;
`;

export { H1, H2 };